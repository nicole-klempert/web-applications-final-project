// single group page management
document.addEventListener("DOMContentLoaded",()=>{
    const groupId=new URLSearchParams(window.location.search).get("id"),currentUser=(localStorage.getItem("loggedInUser")||"").trim();
    if(!groupId){window.location.href="groups.html";return;}
    let group=null;
    const fileToDataURL=file=>new Promise((resolve,reject)=>{const reader=new FileReader();reader.onload=()=>resolve(reader.result);reader.onerror=reject;reader.readAsDataURL(file);});
    const isManager=()=>group&&(group.isOwner||group.isAdmin);
    let selectedPostLocation=null;
    const postLocationPicker=window.PostLocationPicker?.createPicker({buttonId:"group-location-btn",panelId:"group-location-panel",mapId:"group-location-map",searchInputId:"group-location-search",searchButtonId:"group-location-search-btn",clearButtonId:"group-location-clear",labelId:"group-location-selected",onChange:location=>{selectedPostLocation=location;}});
    const loadGroup=async()=>{try{const res=await fetch(`/groups/${groupId}`,{headers:{Accept:"application/json"}}),data=await res.json();if(!res.ok||!data.success)throw new Error(data.error||"Group not found");group=data.group;renderGroup();await loadPosts();}catch(error){document.getElementById("group-loading").textContent=error.message;}};
    const renderGroup=()=>{
        document.getElementById("group-loading").hidden=true;document.getElementById("group-page").hidden=false;document.title=group.name;
        const image=document.getElementById("group-image");image.innerHTML=group.image?`<img src="${group.image}" alt="${group.name}">`:'<i class="bi bi-people-fill"></i>';
        document.getElementById("group-name").textContent=group.name;document.getElementById("group-description").textContent=group.description||"No description yet.";document.getElementById("group-category").innerHTML=`<i class="bi bi-tag"></i> ${group.category||"General"}`;document.getElementById("group-owner").innerHTML=`<i class="bi bi-person-badge"></i> ${group.owner?.username||"Unknown"}`;document.getElementById("group-members-count").innerHTML=`<i class="bi bi-people"></i> ${group.memberCount} members`;
        const role=document.getElementById("group-role");role.textContent=group.isOwner?"Owner":group.isAdmin?"Admin":group.isMember?"Member":"";role.hidden=!role.textContent;
        const membership=document.getElementById("membership-btn");membership.hidden=group.isOwner;membership.textContent=group.isMember?"Leave Group":"Join Group";document.getElementById("edit-group-btn").hidden=!isManager();document.getElementById("group-statistics-btn").hidden=!isManager();document.getElementById("delete-group-btn").hidden=!isManager();document.getElementById("group-composer").hidden=!group.isMember;document.getElementById("admin-panel").hidden=!group.isOwner;
        document.getElementById("group-members").innerHTML=(group.members||[]).map(member=>{const admin=(group.admins||[]).some(a=>String(a._id)===String(member._id));const owner=String(group.owner?._id)===String(member._id);const canRemove=isManager()&&!owner&&(!admin||group.isOwner);return `<div class="group-member"><a href="profile.html?user=${encodeURIComponent(member.username)}"><span class="avatar avatar-purple avatar-sm">${member.username.substring(0,2).toUpperCase()}</span><span>${member.username}</span></a><div>${owner?'<span class="member-badge">Owner</span>':admin?'<span class="member-badge">Admin</span>':''}${group.isOwner&&admin&&!owner?`<button class="mini-btn remove-admin" data-username="${member.username}">Remove Admin</button>`:''}${canRemove?`<button class="mini-btn remove-member" data-id="${member._id}">Remove</button>`:''}</div></div>`;}).join("");
    };
    const loadPosts=async()=>{const container=document.getElementById("group-posts");const res=await fetch(`/posts?group=${encodeURIComponent(group.name)}&limit=50`,{headers:{Accept:"application/json"}}),data=await res.json();if(!data.success)return;container.innerHTML=data.posts.length?data.posts.map(post=>window.createPostCardHTML?window.createPostCardHTML(post):`<article class="post-card" data-post-id="${post._id}"><strong>${post.author}</strong><p>${post.content||""}</p></article>`).join(""):'<p class="group-empty">No posts in this group yet.</p>';if(isManager())container.querySelectorAll(".post-card").forEach(card=>{if(card.querySelector(".delete-post-btn"))return;const header=card.querySelector(".post-card-header");if(header)header.insertAdjacentHTML("beforeend",'<div class="post-actions-right"><button class="delete-post-btn" title="Delete"><i class="bi bi-trash3"></i></button></div>');});};
    document.getElementById("group-statistics-btn").onclick=()=>window.location.href=`statistics.html?groupId=${encodeURIComponent(groupId)}`;
    document.getElementById("membership-btn").onclick=async()=>{const action=group.isMember?"leave":"join",res=await fetch(`/groups/${groupId}/${action}`,{method:"POST",headers:{Accept:"application/json"}}),data=await res.json();if(!res.ok)return alert(data.error||"Action failed");await loadGroup();};
    document.getElementById("group-post-publish").onclick=async()=>{const text=document.getElementById("group-post-text"),file=document.getElementById("group-post-media").files[0],error=document.getElementById("group-post-error");error.textContent="";if(!text.value.trim()&&!file){error.textContent="Post cannot be empty";return;}let mediaUrl="",mediaType="";if(file){mediaUrl=await fileToDataURL(file);mediaType=file.type.startsWith("video/")?"video":"image";}const body={content:text.value.trim(),mediaUrl,mediaType,authorProfilePic:localStorage.getItem("userProfilePic")||"",groupId,...(selectedPostLocation?{location:selectedPostLocation}:{})};const res=await fetch("/posts",{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify(body)}),data=await res.json();if(!res.ok){error.textContent=data.error||"Could not publish";return;}text.value="";document.getElementById("group-post-media").value="";postLocationPicker?.clear();selectedPostLocation=null;const locationPanel=document.getElementById("group-location-panel");if(locationPanel)locationPanel.hidden=true;await loadPosts();};
    const modal=document.getElementById("edit-group-modal"),closeModal=()=>modal.classList.remove("active");document.getElementById("edit-group-btn").onclick=()=>{document.getElementById("edit-group-name").value=group.name;document.getElementById("edit-group-description").value=group.description||"";document.getElementById("edit-group-category").value=group.category||"";document.getElementById("edit-group-image").value=group.image||"";document.getElementById("edit-group-public").checked=group.isPublic!==false;modal.classList.add("active");};document.getElementById("close-edit-group").onclick=closeModal;document.getElementById("cancel-edit-group").onclick=closeModal;
    document.getElementById("edit-group-form").onsubmit=async e=>{e.preventDefault();const error=document.getElementById("edit-group-error"),body={name:document.getElementById("edit-group-name").value.trim(),description:document.getElementById("edit-group-description").value.trim(),category:document.getElementById("edit-group-category").value.trim(),image:document.getElementById("edit-group-image").value.trim(),isPublic:document.getElementById("edit-group-public").checked};const res=await fetch(`/groups/${groupId}`,{method:"PUT",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify(body)}),data=await res.json();if(!res.ok){error.textContent=data.error||"Could not save";return;}closeModal();await loadGroup();};
    document.getElementById("delete-group-btn").onclick=async()=>{if(!confirm(`Delete ${group.name}? All group posts will also be deleted.`))return;const res=await fetch(`/groups/${groupId}`,{method:"DELETE",headers:{Accept:"application/json"}}),data=await res.json();if(!res.ok)return alert(data.error||"Could not delete group");window.location.href="groups.html";};
    document.getElementById("add-admin-btn").onclick=async()=>{const input=document.getElementById("admin-username"),error=document.getElementById("admin-error");const res=await fetch(`/groups/${groupId}/admins`,{method:"POST",headers:{"Content-Type":"application/json",Accept:"application/json"},body:JSON.stringify({username:input.value.trim()})}),data=await res.json();if(!res.ok){error.textContent=data.error||"Could not add admin";return;}input.value="";error.textContent="";await loadGroup();};
    document.addEventListener("click",async e=>{const removeMember=e.target.closest(".remove-member"),removeAdmin=e.target.closest(".remove-admin"),deletePost=e.target.closest(".delete-post-btn"),like=e.target.closest(".like-btn"),commentBtn=e.target.closest(".comment-submit-btn");if(removeMember){if(!confirm("Remove this member?"))return;const res=await fetch(`/groups/${groupId}/members/${removeMember.dataset.id}`,{method:"DELETE",headers:{Accept:"application/json"}}),data=await res.json();if(!res.ok)return alert(data.error||"Could not remove member");await loadGroup();return;}if(removeAdmin){const res=await fetch(`/groups/${groupId}/admins/${encodeURIComponent(removeAdmin.dataset.username)}`,{method:"DELETE",headers:{Accept:"application/json"}}),data=await res.json();if(!res.ok)return alert(data.error||"Could not remove admin");await loadGroup();return;}const postCard=e.target.closest(".post-card"),postId=postCard?.dataset.postId;if(deletePost&&postId){if(!confirm("Delete this post?"))return;const res=await fetch(`/posts/${postId}`,{method:"DELETE",headers:{Accept:"application/json"}});if(!res.ok){const data=await res.json();return alert(data.error||"Could not delete post");}await loadPosts();return;}if(like&&postId){await fetch(`/posts/${postId}/like`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({username:currentUser})});await loadPosts();return;}if(commentBtn&&postId){const input=postCard.querySelector(".comment-input");if(!input?.value.trim())return;await fetch(`/posts/${postId}/comments`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({author:currentUser,authorProfilePic:localStorage.getItem("userProfilePic")||"",text:input.value.trim()})});await loadPosts();}});
    loadGroup();
});
